import "std/c/mlkmeans"
use mlkmeans

test "two blobs cluster"
    create points = list of 0, 0, 1, 0, 8, 8, 9, 8
    create centroids = kmeans(points, 2, 2)
    create labels = assignClusters(points, centroids, 2)
    create sizes = clusterSizes(labels, 2)
    assert (sizes at 0) + (sizes at 1) is equal to 4
    assert inertia(points, centroids, 2) is less than 5
end

test "elbow prefers meaningful k"
    create points = list of 0, 0, 1, 0, 0, 1, 8, 8, 9, 8, 8, 9
    create k = elbowK(points, 3, 2)
    assert k is greater than 1
    assert k is less than 4
end
