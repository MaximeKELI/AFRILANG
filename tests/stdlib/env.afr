import "std/env"
use env

test "env set get has"
    assert set("AFRILANG_P6_ENV", "ok") is equal to true
    assert has("AFRILANG_P6_ENV") is equal to true
    assert get("AFRILANG_P6_ENV") is equal to "ok"
    assert has("AFRILANG_P6_ENV_MISSING_XYZ") is equal to false
end
