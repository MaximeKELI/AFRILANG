// Gap 7 — allowlisted libm under AFRILANG_ALLOW_FFI=1
extern from "m" function sqrt(x number) returns number
extern from "m" function fabs(x number) returns number

test "ffi math smoke"
    assert sqrt(16) == 4
    assert fabs(0 - 3) == 3
end
