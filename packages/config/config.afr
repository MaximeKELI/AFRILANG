module Config
    function getOr(text value, text fallback) returns text
        if value is equal to "" then
            return fallback
        end
        return value
    end

    function key(text name, text value) returns text
        return name + ":" + value
    end
end
