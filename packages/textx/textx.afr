module textx
    function isBlank(s text) returns bool
        return s is equal to ""
    end

    function nonEmpty(s text) returns bool
        return s is not equal to ""
    end

    function label(name text, value text) returns text
        return name + "=" + value
    end
end
