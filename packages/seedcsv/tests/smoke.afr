import "pkg/seedcsv/seedcsv.afr"
use seedcsv
import "std/str"
use str

test "seeds"
    create u = demoUsers()
    assert startsWith(u, "id,name,city")
    assert contains(u, "Lome")
    assert dataRows(u) is equal to 3
    assert dataRows(demoTxXof()) is equal to 2
    assert contains(demoMarkets(), "Sandaga")
end
