module stopwatch
    function begin() returns int
        return 0
    end

    function pulse(t int) returns int
        return t + 1
    end

    function elapsedTicks(started int, now int) returns int
        return now - started
    end
end
