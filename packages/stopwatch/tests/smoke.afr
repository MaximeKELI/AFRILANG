import "pkg/stopwatch/stopwatch.afr"

use stopwatch

test "stopwatch user module"
    create t = begin()
    create t2 = pulse(t)
    create t3 = pulse(t2)
    assert elapsedTicks(t, t3) is equal to 2
end
