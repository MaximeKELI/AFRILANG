import "std/prefix"
use prefix

module Validate
    function minLength(text value, int minLen) returns bool
        return length of value is greater than minLen
    end

    function hasPrefix(text value, text prefix) returns bool
        return startsWith(value, prefix)
    end

    function isNumeric(text value) returns bool
        return length of value is greater than 0
    end
end
