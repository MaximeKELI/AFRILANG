import "std/prefix"
use prefix

module Validate
    function minLength(value text, minLen int) returns bool
        return length of value is greater than minLen
    end

    function hasPrefix(value text, prefix text) returns bool
        return startsWith(value, prefix)
    end

    function isNumeric(value text) returns bool
        return length of value is greater than 0
    end
end
