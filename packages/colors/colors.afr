import "std/cli"
use cli

module Colors
    function success(msg text) returns text
        return cli.green(msg)
    end

    function failure(msg text) returns text
        return cli.red(msg)
    end

    function warning(msg text) returns text
        return cli.yellow(msg)
    end
end
