import "pkg/resultx/resultx.afr"

use resultx

test "resultx helpers"
    assert okValue(5) is equal to 5
    assert fallback(0, 99) is equal to 99
    assert fallback(3, 99) is equal to 3
    assert mapDouble(4) is equal to 8
    assert isZero(0) is equal to true
end
