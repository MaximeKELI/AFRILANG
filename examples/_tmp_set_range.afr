generator function range(n int) returns list int
    create i int = 0
    while i is less than n do
        yield i
        set i = i + 1
    end
end

create seq list int = range(3)
say seq at 0
set seq = range(2)
say seq at 1
