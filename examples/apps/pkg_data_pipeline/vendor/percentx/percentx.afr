module percentx
    function percent(part number, whole number) returns number
        if whole is equal to 0 then
            return 0
        end
        return (part * 100) / whole
    end

    function ofTotal(pct number, total number) returns number
        return (pct * total) / 100
    end
end
