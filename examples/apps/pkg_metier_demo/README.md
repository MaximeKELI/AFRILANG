# pkg_metier_demo

Démo des paquets métier AFRILANG : `httpx`, `geox`, `datax`, `webapi`, `storex`, `deployx`.

```bash
cd examples/apps/pkg_metier_demo
afrilang pkg install
afrilang run .
```

Sans appels réseau (sauf si vous ajoutez `healthCheck` / `postJson`).
