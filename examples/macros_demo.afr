// Macros MVP : définition + appel avec !
// Voir docs : /docs/macros/

macro shout(msg)
    say msg
end

macro assertEq(a, b)
    assert a is equal to b
end

shout!("macros ok")
assertEq!(2 + 2, 4)
shout!("deux plus deux égal quatre")
