final class Config
    property text name
    end

    function init(n text)
        set this.name to n
    end

    destroy
        say "config destroyed"
    end
end

create cfg = new Config("app")
say cfg.name
set cfg.name to "server"
say cfg.name

say "poo advanced complete"
