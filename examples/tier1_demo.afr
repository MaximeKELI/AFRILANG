import "std/json"

create age int = 42
create pi number = 3.14
say age
say pi

create doc json = json.parse("{\"name\": \"AFRILANG\", \"version\": 1}")
say json.getString(doc, "name")
say json.getInt(doc, "version")

create built json = json.makeObject("status", "ok")
say json.stringify(built)
