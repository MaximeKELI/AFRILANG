# Module std/json — bibliothèque standard AFRILANG
#
# import "std/json"
# use json
#
# Fonctions:
#   parse(text text) returns text
#   stringify(value text) returns text
#   getString(text text, key text) returns text
#   getNumber(text text, key text) returns number
#   makeObject(key text, value text) returns text
