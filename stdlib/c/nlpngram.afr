// experimental catalog — IDE signatures only (not core runtime)
module nlpngram

function charNgrams(s text, n number) returns list text
end

function wordNgrams(s text, n number) returns list text
end

function ngramCount(s text, n number) returns number
end

function ngramJaccard(a text, b text, n number) returns number
end

end
