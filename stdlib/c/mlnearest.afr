// experimental catalog — IDE signatures only (not core runtime)
module mlnearest

function nearestIdx(query list number, matrix list number, dims number) returns number
end

function nearestDist(query list number, matrix list number, dims number) returns number
end

function knnIndices(query list number, matrix list number, dims number, k number) returns list number
end

function batchNearest(queries list number, matrix list number, dims number) returns list number
end

end
