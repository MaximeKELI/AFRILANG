// experimental catalog — IDE signatures only (not core runtime)
module gameultra408

function astarGridPath(grid list number, w number, h number, sx number, sy number, gx number, gy number, blockedAbove number) returns list number
end

function floodCount(grid list number, w number, h number, sx number, sy number, blockedAbove number) returns number
end

function simplifyGridPath(path list number, w number) returns list number
end

end
