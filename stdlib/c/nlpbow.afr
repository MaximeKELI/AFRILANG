// experimental catalog — IDE signatures only (not core runtime)
module nlpbow

function bagOfWords(doc text, vocab list text) returns list number
end

function vocabFrom(docs list text) returns list text
end

function bowDot(a list number, b list number) returns number
end

function bowCosine(a list number, b list number) returns number
end

function termFrequency(doc text, term text) returns number
end

end
