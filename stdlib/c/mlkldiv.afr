// experimental catalog — IDE signatures only (not core runtime)
module mlkldiv

function klDiv(p list number, q list number) returns number
end

function jsDiv(p list number, q list number) returns number
end

function klDivBits(p list number, q list number) returns number
end

function symmetricKl(p list number, q list number) returns number
end

function hellinger(p list number, q list number) returns number
end

end
