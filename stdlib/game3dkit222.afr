module game3dkit222

function dot3(ax number, ay number, az number, bx number, by number, bz number) returns number
end

function crossX(ax number, ay number, az number, bx number, by number, bz number) returns number
end

function crossY(ax number, ay number, az number, bx number, by number, bz number) returns number
end

end
