# Module std/io — bibliothèque standard AFRILANG
#
# import "std/io"
# use io
#
# Fonctions:
#   readFile(path text) returns text
#   writeFile(path text, content text)
#   fileExists(path text) returns bool
#   readLine() returns text
