module graphbellman

function bellmanFord(adj list number, n number, start number) returns list number
end

function hasNegCycle(adj list number, n number) returns bool
end

function negEdgeCount(adj list number, n number) returns number
end

function relaxOnce(adj list number, n number, dist list number) returns list number
end

function pathCost(adj list number, n number, path list number) returns number
end

function reachableWeighted(adj list number, n number, start number) returns number
end

end
