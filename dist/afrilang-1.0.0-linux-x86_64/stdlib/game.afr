module game

function scoreBonus(score number, level number) returns number
end

function levelFromXp(xp number) returns number
end

function xpForScore(score number) returns number
end

function speedMsForLevel(level number) returns number
end

end
